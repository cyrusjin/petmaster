.merchant-tab-bar {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  display: flex;
  height: calc(100rpx + env(safe-area-inset-bottom));
  padding-bottom: env(safe-area-inset-bottom);
  background: #fff;
  border-top: 1rpx solid #F2DEC9;
  box-shadow: 0 -4rpx 24rpx rgba(0, 0, 0, 0.04);
  z-index: 10000;
  box-sizing: border-box;
}
.tab-item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  color: #999999;
}
.tab-item.active { color: #E98657; }
.tab-icon-img {
  width: 44rpx;
  height: 44rpx;
  display: block;
}
.tab-text { font-size: 22rpx; margin-top: 6rpx; font-weight: 500; }
.merchant-page {
  min-height: 100vh;
  box-sizing: border-box;
}
/* 内容区底部 padding：躲开 fixed Tab（约 100rpx）并再留出呼吸空间 */
.merchant-page .container {
  padding-bottom: 160rpx !important;
  box-sizing: border-box;
}
.merchant-tab-spacer {
  width: 100%;
  height: 40rpx;
  flex-shrink: 0;
}
